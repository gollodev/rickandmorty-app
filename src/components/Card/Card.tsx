import { FC } from 'react'; 

interface CardProps {};

const Card: FC<CardProps> = ({}): JSX.Element => {
    return (
        <div className="Card">
            Card
        </div>
    );
};

export default Card;